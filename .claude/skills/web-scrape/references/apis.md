# APIs — see the shared repo-root references instead

Moved 2026-07-27: Exa and Firecrawl are shared infrastructure other future
skills will also use, not private to `web-scrape` — same reasoning as the
`.env` move. Full endpoint documentation now lives at:

- `references/exa-api.md`
- `references/firecrawl-api.md`

(both at the repo root, same place as `references/clickup-api.md`)
